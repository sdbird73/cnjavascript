// let favouriteSongs = [
//     "Blur - When",
//     "Oasis - Never",
//     "ABBA - Blue"
//     ];
// console.log(favouriteSongs [2]);

// favouriteSongs[1] = "The Who - Blank"
// console.log(favouriteSongs);

// console.log(favouriteSongs.length);

// favouriteSongs.push ("Blondie - Atomic");
// console.log(favouriteSongs);

// favouriteSongs.pop ()
// console.log(favouriteSongs);


let favouriteWebsites = [
    "Amazon",
    "The Guardian",
    "BBC"
];
console.log(favouriteWebsites);

// favouriteWebsites.push ("The Telegraph");
// favouriteWebsites.push ("Sky")
// console.log(favouriteWebsites);

// favouriteWebsites.pop ();
// console.log(favouriteWebsites);

// favouriteWebsites.shift()
// console.log(favouriteWebsites);
// favouriteWebsites.unshift("M&S")
// console.log(favouriteWebsites);

//unshift() and shift() work in exactly the same way as push() and pop(), respectively, except that they work on the beginning of the array, not the end.

//The splice() method can be used to add new items to an array (position, no. removed, "item") and delete.
// favouriteWebsites.splice (1, 1);
// console.log(favouriteWebsites);

// The slice() method slices out a piece of an array temporarily.
console.log(favouriteWebsites.slice(1));

console.log(favouriteWebsites);








